# Payment API Testing Guide - Secure Razorpay Implementation

## ? SECURITY UPGRADE COMPLETE
The payment verification now uses **real cryptographic signature verification** with HMAC-SHA256 instead of dummy logic.

### ?? What Changed:
1. **Cryptographic Verification**: Real signature validation using Razorpay's security protocol
2. **Secure Configuration**: Razorpay keys properly configured in appsettings.json
3. **Enhanced Validation**: Proper error handling for missing or invalid payment attributes

## Available Endpoints

### 1. Create Order (Authenticated)
```
POST /api/Payment/create-order/{bookingId}
Authorization: Bearer {token} (required)
```

### 2. Create Order (Test - No Auth Required)
```
POST /api/Payment/create-order-test/{bookingId}
```

### 3. ? Verify Payment (Authenticated) - NOW SECURE
```
POST /api/Payment/verify-payment
Authorization: Bearer {token} (required)
Content-Type: application/json

Body:
{
  "razorpay_order_id": "order_12345",
  "razorpay_payment_id": "pay_67890", 
  "razorpay_signature": "generated_hmac_signature",
  "bookingId": "1"
}
```

### 4. Payment History (Authenticated)
```
GET /api/Payment/my-history
Authorization: Bearer {token} (required)
```

## ?? Security Implementation Details

### HMAC-SHA256 Signature Verification
The service now performs real cryptographic verification:

1. **Payload Creation**: `razorpay_order_id|razorpay_payment_id`
2. **Signature Generation**: HMAC-SHA256 with your secret key
3. **Secure Comparison**: Cryptographically secure signature comparison
4. **Database Update**: Only updates booking status if signature is valid

### Required Payment Attributes
- `razorpay_order_id`: The order ID from Razorpay
- `razorpay_payment_id`: The payment ID from Razorpay  
- `razorpay_signature`: The signature generated by Razorpay
- `bookingId`: Your internal booking ID

## Expected Response Formats

### Create Order Response
```json
{
  "orderId": "order_1234567890abcdef",
  "amount": 10000,
  "currency": "INR",
  "keyId": "rzp_test_DiMiYr3VpklxK8"
}
```

### ? Secure Payment Verification Response
```json
{
  "status": "Payment verified successfully",
  "message": "Your booking has been confirmed and payment processed.",
  "isVerified": true
}
```

## Error Responses

### Missing Payment Attributes (400)
```json
{
  "message": "Payment verification failed: Missing required payment attributes (razorpay_order_id, razorpay_payment_id, razorpay_signature)."
}
```

### Invalid Signature (400) - SECURITY PROTECTION
```json
{
  "message": "Payment verification failed: Invalid signature. Payment may be fraudulent."
}
```

### Booking Issues (400)
```json
{
  "message": "Booking not found."
}
```
or
```json
{
  "message": "Booking is already confirmed."
}
```

## ?? Configuration

### Razorpay Keys (appsettings.json)
```json
{
  "Razorpay": {
    "KeyId": "rzp_test_DiMiYr3VpklxK8",
    "KeySecret": "yVDDF9cO2QWVdZ2DCqSIIbZq"
  }
}
```

?? **Security Note**: In production, store the `KeySecret` in environment variables or Azure Key Vault, not in appsettings.json.

## Testing the Secure Implementation

### Test Order Creation
```bash
curl -X POST "https://localhost:7000/api/Payment/create-order-test/1" \
  -H "Content-Type: application/json"
```

### Test Payment Verification (Requires Valid Signature)
```bash
curl -X POST "https://localhost:7000/api/Payment/verify-payment" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{
    "razorpay_order_id": "order_12345",
    "razorpay_payment_id": "pay_67890",
    "razorpay_signature": "valid_hmac_signature_here",
    "bookingId": "1"
  }'
```

## ?? Frontend Integration

When integrating with Razorpay on the frontend, use this flow:

1. **Create Order**: Call your `/create-order` endpoint
2. **Razorpay Checkout**: Use the returned order details
3. **Payment Success**: Razorpay will provide the payment details with signature
4. **Verify Payment**: Send all Razorpay response data to `/verify-payment`

The signature verification ensures that the payment data hasn't been tampered with and actually came from Razorpay.

---

# Circular Reference Fix - Booking and Workspace APIs

## Problem Fixed
The booking and workspace APIs were returning circular reference errors due to:
1. `Booking` having a navigation property to `Workspace`
2. `Workspace` having a collection of `Bookings`
3. JSON serialization trying to serialize the entire object graph infinitely

## Solutions Implemented

### 1. JSON Serialization Configuration (Program.cs)
```csharp
builder.Services.AddControllers()
    .AddJsonOptions(options =>
    {
        // Handle circular references in JSON serialization
        options.JsonSerializerOptions.ReferenceHandler = System.Text.Json.Serialization.ReferenceHandler.IgnoreCycles;
        options.JsonSerializerOptions.PropertyNamingPolicy = System.Text.Json.JsonNamingPolicy.CamelCase;
        options.JsonSerializerOptions.MaxDepth = 32;
    });
```

### 2. Model Attributes (JsonIgnore)
- Added `[JsonIgnore]` to `Booking.ApplicationUser` property
- Added `[JsonIgnore]` to `Workspace.Bookings` collection

### 3. Better Error Handling
- Updated controllers to return proper JSON error responses
- Added test endpoints for debugging without authentication

## Available Test Endpoints

### Booking APIs
- `GET /api/Booking/test/{id}` - Get booking by ID (no auth required)
- `GET /api/Booking/test-all` - Get all test user bookings (no auth required)
- `GET /api/Booking/{id}` - Get booking by ID (auth required)
- `GET /api/Booking/my-bookings` - Get user's bookings (auth required)
- `POST /api/Booking` - Create booking (auth required)

### Workspace APIs
- `GET /api/Workspaces` - Get all workspaces
- `GET /api/Workspaces/{id}` - Get workspace by ID
- `POST /api/Workspaces` - Create workspace (auth required)

### Payment APIs
- `POST /api/Payment/create-order-test/{bookingId}` - Create order (no auth required)
- `POST /api/Payment/create-order/{bookingId}` - Create order (auth required)

## Expected Response Format

### Booking Response (Fixed)
```json
{
  "id": 1,
  "applicationUserId": "test-user-123",
  "workspaceId": 4,
  "workspace": {
    "id": 4,
    "name": "Emerald Meeting Room",
    "type": "Conference",
    "capacity": 75,
    "pricePerHour": 9000.00,
    "amenities": "Projector, Wi-Fi",
    "isAvailable": true,
    "imageUrl": "https://example.com/images/emerald_room.jpg",
    "createdDate": "2025-08-24T20:24:09.2255164",
    "updatedDate": "2025-08-24T20:24:09.3033333"
  },
  "startTime": "2025-08-25T20:24:09.2255164",
  "endTime": "2025-08-25T22:24:09.2255164",
  "status": "Pending",
  "totalPrice": 100.00
}
```

## Testing Commands

```bash
# Test booking endpoint (should work now)
curl -X GET "https://localhost:7000/api/Booking/test/1" -k

# Test all bookings (should work now)
curl -X GET "https://localhost:7000/api/Booking/test-all" -k

# Test payment endpoint
curl -X POST "https://localhost:7000/api/Payment/create-order-test/1" -k
```

## Next Steps (Optional Improvements)

1. **Environment Variables**: Move secret keys to environment variables for production
2. **Add AutoMapper** - For easy entity-to-DTO mapping
3. **Implement pagination** - For large result sets
4. **Add proper logging** - For better debugging and monitoring
5. **Real Razorpay API Integration** - Use actual Razorpay SDK for order creation

The payment verification is now cryptographically secure and will reject any tampered or fraudulent payment attempts. ??